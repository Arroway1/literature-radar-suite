# 中文说明

根目录 <a href="README.md">README.md</a> 已包含完整的中文安装、更新、使用、依赖、隐私和致谢说明。

为避免维护两份不同版本的文档，<code>README.md</code> 是唯一维护源。公开 README 不包含任何用户个人的关键词、期刊、数量上限、邮箱或输出目录配置；这些内容应由用户在自己的请求或配置文件中提供。

其他 Agent 可使用安装脚本的 <code>-TargetSkillsRoot</code> 参数指定其 Skill 根目录；不支持 <code>SKILL.md</code> 的 Agent 需要按自身格式转换。
